{
  "version": 2,
  "buildCommand": "npm run install:all && npm run build:all",
  "builds": [
    { "src": "server.js", "use": "@vercel/node" }
  ],
  "routes": [
    { "src": "/(.*)", "dest": "server.js" }
  ]
}
